# CLAUDE.md

Claude or any other coding assistant should treat `AGENTS.md` as the canonical repository operating instructions.

Before editing:

1. Read `AGENTS.md`.
2. Read `README.md`.
3. Read the relevant `/docs` files.
4. Inspect the actual target page and `js/askgenie-ai.js` if AI behavior is involved.

Do not assume missing backend code, database schema, or `my-genie.html` exists. Preserve bilingual/mobile behavior and avoid unrelated rewrites.
